# Configure server secrets through Sites, never in the browser bundle.
KIE_API_KEY=
CARTESIA_API_KEY=
CARTESIA_SIA_VOICE_ID=
CARTESIA_API_VERSION=
CARTESIA_MODEL=sonic-3.6
# Generation remains off until keys, approved image, live API test and budget are checked.
GENERATION_ENABLED=false
MAX_DAILY_GENERATIONS=3
